spec:
  template:
    spec:
      containers:
      - name: vote
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "250m"
            memory: "200Mi"